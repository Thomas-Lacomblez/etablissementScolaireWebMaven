// Declarative //
pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				sh 'mvn install'
			}
		 post {
                success {
                    junit 'EtablissementScolaire-service/target/surefire-reports/*.xml'
                }
            }   
		}
	}
